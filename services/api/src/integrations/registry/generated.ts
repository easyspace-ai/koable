// Stub: overwritten by tools/generate-registry.ts when run. Without
// that generator, the curated category files alongside this one are
// the catalog; index.ts prunes any uninstalled piecePackages.
import type { IntegrationDefinition } from "../types.js";

export const GENERATED_REGISTRY: Record<string, IntegrationDefinition> = {};
