apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: doable
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # storageClassName left empty → cluster default (gp3 on EKS, pd-balanced on GKE, etc.)
  # storageClassName: ""
